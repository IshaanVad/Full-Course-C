#include <stdio.h>

int main() {

    // THIS IS A COMMENT
    // use this to comment on any code you created

    /* This
       is a
       multline comment
    */

    /* 
       escape sequence = character combination consisting of a backslash \
                         followed by a letter or combination of digits.
                         They specify actions within a line or string of text.
                         \n = new line
                         \t = tab
    */

   printf("I\nlike\nprogramming.\n"); // makes a new line after every word
   printf("\tThis is interesting.\n"); // tabs before "This"
   printf("\"I like pizza\" - me"); // use \" for any quotations in a String

    return 0;
}