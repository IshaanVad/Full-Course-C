#include <stdio.h>

int main() {

    // for loop = repeats a section of code a limited amount of times

    for (int i = 0; i < 10; i++) { // repeats 10 times (0 indexing)
        printf("%d\n", i);
    }

    return 0;
}